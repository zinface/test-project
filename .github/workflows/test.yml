name: Test Action

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-20.04
        
    steps:
      - run: cho "Hello World"
      - run: cat /etc/os-release
      - run: lsb_release -a
      - run: arch
      - run: uname -a

